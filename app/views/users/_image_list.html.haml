
.link-to-uploads
  = link_to "Upload an image", '/image'

.images
  - current_user.images.each do |image|
    .image-container
      %h4
        = image.title
      %p
        = image_tag image.blob.url(:thumb)
